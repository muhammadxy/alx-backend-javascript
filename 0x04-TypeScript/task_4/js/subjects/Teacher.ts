/**
 * Teacher interface that requires:
 * - firstName : string
 * - lastName : string
 */
namespace Subjects {
    export interface Teacher {
        firstName: string;
        lastName: string;
    }
}
